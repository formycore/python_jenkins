pipeline {
    agent any
    stages {
        stage('Git clone'){
            steps {
                sh 'python3 -v'
                sh 'python3 -m pip --version'
                echo 'this is for testing'
            }
        }
    }
}